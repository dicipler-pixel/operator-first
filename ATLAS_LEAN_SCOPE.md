# Atlas forcing formalization

These additions formalize the exact algebra behind AK_Atlas_Probe.zip. Verification is determined by the successful CI run for the exact source revision recorded in PR #4. The prior 51-theorem result does not by itself certify these additions.

- `KakeyaForcingLinear`: forcing as failure of kernel inclusion; an explicit linear dual obstruction; invariance under an invertible change of coefficient coordinates and nonzero rescaling of the target; the action of a shifted operator and its inverse on a kernel vector; a real inner-product projector criterion with symmetry, kernel-image and kernel-fixing hypotheses; rational label normalization identities.
- `KakeyaAtlasFamily`: actual seven generator rows and their rational linear combination; the permissible first-step parameters; the q=1 stall; inductive reachability allowing every fresh-vertex order and arbitrary rational coefficients; completion if and only if q=2. The four successful steps are given explicitly. Graph incidences and seed locations are fixed throughout this [2,2] family. q=-1 is an inadmissible edge label for the Kakeya application; the algebraic exclusion also covers it.
- `KakeyaAtlasProjector`: the initial g3 kernel at q=2 is exactly the line spanned by (-1,-1,1,-1,2,-1,-2); the rational operator P=c c^T/13 is symmetric, idempotent, has image in that kernel, and fixes it; its target response is exactly 1/13 and positive. Its image contains the explicit first forcing witness.

The family classification is not a finite parameter sweep, a fixed-order check, or a premise that assumes completion. Its exclusion quantifies over the whole rational family, all coefficient vectors, and all sequences generated by the stated forcing rule.

## Boundaries

No new Kakeya exponent: the only completing member of this fixed family has score 7/4, above 67/40. These modules do not certify a search over other graph topologies, graph constructibility in a general grammar, the full subset-rank theorem, or the integer-clearing theorem for arbitrary rational families. The already-verified q=2 integer witnesses remain in KakeyaSeedingControl.

No contour-integral or analytic-residue theorem is claimed. The general inverse-on-kernel lemma and the exact rational projector value establish the algebraic parts of the spectral interpretation. Identifying the projector with a Riesz contour integral requires additional analytic formalization. No physical photon/electron identification is asserted.

The negative controls additionally require the attempted claims Complete(1) and target-response=0 to fail mathematically. Existing compiler, axiom-audit and leanchecker gates also run for every new module.
