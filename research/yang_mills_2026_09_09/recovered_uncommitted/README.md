# Recovered, previously unattached-tree source

These exact six blobs were created during the original September 9 session in tree `cce4c5310b097f284932e2fc71364cb63c4538fd`, but that tree was never attached to the research branch. They are now preserved here by the later full-report audit, without pretending they were previously committed or completed.

This directory is a forensic partial source archive, not an independent runnable project. Its scripts depend on the original workstream modules. The full-report evidence artifact assembles them with the published scripts in `rerun_work/` and retains the original publication snapshot.

Do not run the recovered aggregate `run_all.py` as a completed release: its required `scripts/two_sided_targets.json` was not recovered. The complete two-sided enclosure task remains unverified. The separately recovered local-window, scaling, spatial-incidence, and numerical control programs were successfully replayed by report-audit run 34380125783. They are not new searches or new Lean compilations.

See `../reports/04_full_session_report.md` for the corrected record and missing proof-document list.
