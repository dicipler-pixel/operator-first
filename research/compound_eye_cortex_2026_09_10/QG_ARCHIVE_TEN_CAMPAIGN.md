# QG Archive Ten — Compound Eye / Cortex campaign

**Campaign:** `QG-ARCHIVE-TEN-01`  
**Date:** 10 September 2026  
**First run:** PASS, 22 finite/exact controls.

## Main pattern

The archive does not mainly show that old invariants were useless. It shows that **each compression forgets different structure**.

- Full local Dirac crossing: signed branches `+1,-1`, but net spectral flow `0`. Keep branch history; Mixer Memory flags `SIGN_CANCELLATION`.
- Isospectral rotation: two 2x2 Hermitian operators have identical spectrum `{-1,+1}` while occupied projectors have `Tr(P0 P1)=1/2` and `||P0-P1||_F^2=1`.
- Opposite projector paths can end at the same projector, so endpoint subspace retains more than the scalar spectrum but less than ordered path memory.

Working retention ladder:

```text
scalar spectrum / eta / net count
        <
projector or subspace endpoint
        <
ordered projector/subspace path
        <
labelled operator + topology + graded/path records
```

This is a diagnostic hierarchy, not a universal total order.

## Ten archive tests

1. **Original gravity proposal — `INTERPRETATION_TARGET_NOT_THEOREM`.** Retain intrinsic operator/spectral response. Do not license the jump to the Einstein tensor; locality, Bianchi, Lorentzian metric, geodesic motion and universal coupling remain missing bridges.
2. **Admissibility — `SCOPED_DEFINITION`.** Retain as a proposed global-consistency constraint, not as something APS forces.
3. **Eta vs spectral flow — `FINITE_CONSEQUENCE_PASS`.** Shifted-circle calibration at `a=1/3`: `eta=1/3`, reduced eta `=1/6`, while a unit parameter shift has integer spectral flow `+1`. The script checks the rational consequences of the standard formula; it does not prove analytic continuation.
4. **Flat T2 Dirac — `FINITE_EXACT_PASS`.** Exact mode-square grid preserves +/- pairing and finds a zero only at `(alpha,beta)=(0,0)` on the sampled fundamental domain.
5. **T2 unit-flow correction — `EXACT_LOCAL_PASS`.** For `D(t)=t sigma_1`, branch flows are `+1,-1`; net flow is zero but two events occurred.
6. **Simply connected boundary — `SCOPED_THEOREM_CONTRACT`.** Trivial `pi1` gives only trivial flat U(1) holonomy; no universal exclusion of other index/spectral structure follows.
7. **T2 minimality — `MODEL_MINIMALITY_PASS`.** For closed orientable surfaces `b1=2g`; genus one is the first genus with two independent H1 cycles. This is model-class minimality, not unique physical substrate.
8. **Knot complements — `THEOREM_CONTRACT_READY`.** Preserve the ordinary knot exterior contract `boundary=T2`, `H1=Z`, `H2=0`; no triangulation proof is fabricated by the finite runner.
9. **8_19 — `EXTERNAL_SOURCE_CHECKPOINT`.** Retain as `T(4,3)` and as a graded/Khovanov-rich example, not a hyperbolic/Mostow example.
10. **Khovanov proxy — `IDENTIFICATION_REFUSED`.** A graded-chain Laplacian/operator remains distinct from an analytic APS boundary Dirac unless an explicit bridge is supplied.

## Retina / lineage result

The public retina already has the 22-eye `ce.set.knotlab@1.0.0`. It should be viewed with projector, shape-transport, time-memory, forcing/observability and operator/resolvent families rather than collapsed into one vote.

`ce.knotlab.eta`, `ce.knotlab.spectral_flow` and `ce.knotlab.aps_admission` are separate functions carried by the same installed plugin SHA, so they are **shared lineage**, not independent implementations.

The public 191-eye manifest contains no explicit Khovanov eye. In addition, `ce.knotlab.aps_knot_operator@1.0.0` is only `specified` and unimplemented. That is a real sensory gap if we want a native Khovanov-vs-APS comparison rather than a prose quarantine.

## Mixer result

The first archive mix intentionally produced and correctly flagged all three distortion classes:

```text
SIGN_CANCELLATION
SHARED_LINEAGE
MIXED_SCALE
```

The external 8_19 checkpoint was retained as an enabled/no-code-output source rather than fabricated as an executed eye.

## Scientific lesson

The safest surviving old chain is

```text
operator -> projector/subspace -> ordered transport/history -> scoped observable.
```

The repeated old failure mode was reversing a compression without a theorem: `eta -> ontology`, `SF -> electron`, `knot label -> geometry`, or `scalar stress -> Einstein tensor`.

Exact theorem statements are not required to continue this campaign. They are needed when a card is promoted to a theorem-genome/Lean target. Highest-value exact inputs later are the flat-T2 operator/domain, the precise APS eta/SF relation, any actual knot-complement APS operator, the Khovanov complex/Laplacian definition, and the pre-physical spectral-stress functional.