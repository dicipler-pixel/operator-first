# Compound Eye side scanner — integration checkpoint

Requested by Jeromie Beasley after the 19:18 UTC hard backup. The backup branch and its bytes remain unchanged. Main is not modified. Large search jobs are not being restarted by this checkpoint.

The recent search used separate diagnostics, not an attached Compound Eye side scanner. The actual newer Whole View archive has now been recovered from Library: `Compound_Eye_Whole_View_Complete.zip`, 54,473,436 bytes. Its native `instrument/machine.py` is version 1.0.1; loading its actual registry succeeds with 204 eye definitions and 33 set versions. This is a later focused line than Universal 3.2's 191/29 catalog, not a reason to discard either lineage.

The integration uses this real registry, immutable plugin registration, dependency DAG, shared-context execution and evidence records. New discrete graph sets will have their own combined outputs, followed by a whole-picture output. Nonlinear graph diagnostics will not be forced into the linear physical-observation fusion formula or averaged as independent votes.

Planned bounded execution: scan preserved planar endpoints and the closed plateau; inspect legal adjacent flips and relative vertex transpositions; compare defect support, unavoidable edge-replacement cost and explicit coloring witnesses; retain the zero-triple necessary-condition survivor with planarity UNKNOWN. Save recommendations separately from acceptance and record the exact neighborhood covered. Missing path/batch files remain missing.

This is an integration start, not a result announcement. The next checkpoint will contain executed outputs and source hashes.
