The real event handlers were exercised with a minimal Node DOM adapter. This checks event logic and diagram coordinates, not browser layout. Run `node check_atlas_demo.cjs` from any directory, passing the path to that script; it reads its neighboring inputs and writes its evidence alongside them. No Node packages are required. The exact SVG full/reduced network states were rendered and visually inspected. Chromium was unavailable and its download timed out; full-page browser layout remains unverified.
