# Implementation queue

1. Completed in this draft: source snapshot, six paper project pages, seven Atlas cards, intake queue, generated navigation and standard-library catalog validator. Proof files stay at their existing locations.
2. Light pilot: reconcile publication identity; retrieve the complete prior supplement; import the delivered two-frequency/probe addition; link each manuscript statement to its proof or model evidence; build a full current standalone supplement. Formalize new statements only after displaying their complete hypotheses.
3. Remaining active projects: map Offset, Gravity, arithmetic Kakeya, Earth–Moon and CMF releases. Reconcile upstream and follow-up results without double-counting. Attach current exact search certificates where still outside GitHub.
4. Full corpus intake: reconcile the actual 20+ publication records and ZIPs. Confirm each title, version, DOI, source, figures and evidence before adding a complete paper manifest. The current intake leads are not a complete paper count.
5. Tracking: create one tracking issue per reconciled paper and one research board with filtered paper views. Do this from the register to avoid conflicting duplicate inventories. No board or tracking issues are claimed created in this draft.
6. Shared-module extraction: identify actual repeated dependencies; refactor one module at a time on a separate review branch, rerun affected proof gates, and preserve source provenance. Do not reorganize the full corpus in one move.
7. Release automation: add per-paper build manifests, pinned dependency closure, source hashes, artifact checks and checks for stale links/evidence. Then automate the catalog checker in CI.

Completion criterion for a paper project: its identity is reconciled; every central claim has a grade and evidence pointer; reusable results have assumption-bearing cards; its current supplement reproduces without requiring a moving branch; open work is listed explicitly.
