# ACC Target 2 parallel notes

Use the second official Discovery instance exactly as it appears in the SAIR pool.

Use the ACC code in this folder together with the current live engine from the handoff package. Preserve the existing move semantics unless the official verifier shows a mismatch.

Workflow: run raw eyes, independent set outputs, Matrix routing, then the Whole Eye planner lanes if the baseline stalls. Reuse the shared reverse target ball and rank-2 abelian shadow. Replay every candidate under both our engine and the official verifier.

Return the exact challenge ID and relators, best verified path length, primitive move list, verifier result, search statistics, whether optimality is certified, and the best frontier fingerprint if unsolved.
